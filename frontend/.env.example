# CampusFix Frontend — Environment Configuration
# Copy this file to .env and adjust if your backend runs elsewhere.

# Base URL of the Phase 7 backend API, including the version prefix.
VITE_API_URL=http://localhost:5000/api/v1
